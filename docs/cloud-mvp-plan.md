# Ziki Cloud：执行计划与进度

更新：2026-09-13。用户已授权按本文分阶段实现，不再使用目标模式推进。
开发分支：`codex/cloud-subscriptions-mvp`，起点：`a4a11c6`。

## 文档入口（各自维护唯一信息，不互相复制）

- [产品文档](cloud-product.md)：用户、范围、使用规则、产品验收。
- [技术方案](cloud-architecture.md)：系统边界、数据契约、安全、待验证决策。
- [测试与开发方法](cloud-validation.md)：测试映射、原生调试、成本控制。
- 本文件：阶段、进度、外部依赖及实际证据。

## 阶段与退出条件

| 阶段 | 执行内容 | 退出条件 | 状态 |
|---|---|---|---|
| P0 定义 | 产品、技术、测试文档；现状与基线 | 可评审的范围与测试方法，未决问题明确 | 完成，作为实施依据 |
| P1 基础 | cloud 工作区、本地迁移、额度状态机、维护中的认证方案选型 | 迁移可重放；并发/重放不会重复计费或越权 | 完成，本地验证通过 |
| P2 服务 | 隔离 staging、真实登录、云识别/整理、客户端双模式 | 授权样本走完真实链路；BYOK 独立；错误可恢复 | 真实联调前置配置未就绪 |
| P3 支付 | Stripe 测试月订阅、加量、续费、取消与回调 | 测试交易和流水一致，重复/乱序/退款通过 | 未开始 |
| P4 交付 | 双语界面、隐私帮助、原生验收与发布 | 前述证据齐全；价格/许可/正式收费获确认 | 未开始 |

每阶段先定义验收再实现，达标后继续推进，不把中间里程碑当作终点。
只有外部凭证、商业决策或上线授权构成实际阻塞时才交还用户；不冒充功能完成，不改变真实收费或生产配置。

## 当前事实与基线

源码 `a4a11c6`，核验日期 2026-09-13：

- Swift 原生客户端；`ASRSession` 已抽象供应商，整理仍绑定百炼。
- 官网为独立静态 TanStack 站点；连接的 Cloudflare 账户仅有 `ziki-website`，未发现对应业务 D1。
- 仓库没有账号/计费后端，未发现应用级许可证。字体许可证不等于应用许可。
- 本会话没有 Stripe connector；英国商户的测试凭证、Webhook 和结算设置未核验。
- Core harness **90/90**、App harness **14/14** 通过；未运行实时音量测试。
- 官网 build、类型检查、Node **6/6** 通过。
- 原 `.build` 引用改名前路径，首次 `swift test` 因模块缓存失败。正确入口是 README 的 executable harness。
- 改用 `/tmp/ziki-cloud-baseline.k1IyuZ` 后基线通过；原缓存、安装 App、Keychain 和权限未修改。
- 没有录音、付费模型调用、Stripe 交易或新版浏览器业务验收；不把既有官网测试冒充商业版证据。

后续纯文档修改复用这份基线，不重新编译。

## 外部决策门槛

这些事项不阻塞安全的本地基础工作，但在相应阶段必须解决：

- P1：认证实现与必要身份/邮件供应商配置，原生登录安全路径。
- P2：官方模型账户、区域和授权测试预算；禁止自动上传个人 BYOK Key。
- P3：安全配置 Stripe 测试凭证、测试 Price ID；最终价格未定时只用明确标记的测试 fixture。
- P4：月费/额度/试用/加量有效期/部分成功计费，许可证与公私有边界，隐私/税务条款、正式签名公证、真实收费授权。

已向用户询问后端公开/私有范围、实际登录方式与海外模型供应商；未收到决定前，本轮新增后端只在本地提交，不推送公开仓库。
P2 的真实身份/模型凭证尚未配置，不能从个人 Keychain 读取替代。P3 无 Stripe connector/测试凭证；没有创建测试商品或实际交易。

## P1 实施记录（2026-09-13）

- 实现：`cloud/` Workers + D1 本地工作区，2 个迁移，按来源与有效期的授予、原子预留、一次性 provider claim、条件结算/取消/到期、跨用户隔离。
- 认证：Better Auth 1.7.4 设备授权与 bearer 会话，D1 schema drift、撤销、未验证邮箱、Origin、实际流式 body 上限及设备码单次领取测试。真实登录供应商与批准 UI 尚未接入。
- 运行时：Wrangler 4.131.1 / workerd 2026-09-11；当天 2026-09-13 compatibility date 尚不被已发布运行时支持，已对齐测试 pool 依赖，不跳过测试。
- 本地 D1 两次迁移：首次成功，第二次无待应用项。TypeScript 与 Worker dry-run build 通过；npm audit 为 0 项。
- 账本/认证共 **23/23** 个用例通过（认证与额外竞态 10 项；账本 13 项）。集成检查发现测试把随机 grant UUID 的字典序误当成额度到期序；已改为按分配 position 查询，定向重跑受影响 13 项通过，未重复运行不受影响用例。不是账本扣款错误。
- 原生：`outputs/Ziki-Dev.app` Debug 构建、plist/资源/签名验证、App harness 14/14 通过。开发设置、Keychain、历史和诊断隔离，禁用正式 updater；旧开发包备份保留。
- 分工：两个 `gpt-5.6-luna` 子代理分别处理开发包隔离和账本测试；主代理实现/复核账务、认证、安全与额外竞态用例。
- 未验证/未执行：真实身份登录、云模型质量与成本、Stripe、云部署、正式 App 启动/录音/权限、main 合并、Release、替换 `/Applications/Ziki.app`。官网未改，复用既有官网证据。

## 下一步与证据格式

按 P1 → P4 实施；独立机械任务交给低阶子代理，鉴权与计费不变量由主代理负责复核。
后续只追加简短记录：源码版本 → 环境 → 用例 → 结果 → 未覆盖项。
实现、本地测试、外部验证、main 集成、部署、Release 分别报告。
